перенаправление вывод tasks на другой сервер 
delegate_to -> директива, которая указывает, где только запустить эту команду

// команда запускается на каждом хосте, но потом делегируется на целевой хост